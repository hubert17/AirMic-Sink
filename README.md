# AirMic-Sink
Zero-compromise remote microphone streaming pipeline. Uses a lightweight ASP.NET Core signaling hub and a low-overhead native receiver to pipe real-time remote audio directly into a Virtual Audio Device.
